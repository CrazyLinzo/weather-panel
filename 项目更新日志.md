# 项目更新日志

> 记录农业气象智能监测面板的数据动态化现状、可演进方向与后续维护优先级。

## 当前架构概览

项目采用“**运行时天气数据 + 静态研究内容/配置 + 自动更新的外部图像**”的混合模式：

- `fieldsight.html`：白色研究简报风主页面，实时天气监测与研究内容展示。
- `index.html`：暗色仪表盘版本，存在部分静态研究数据版本滞后。
- `fieldsight-data.js` / `data.js`：产区、气候指数、新闻、事件、图源等前端静态配置。
- `fetch_data.py` 与根目录 CSV：离线数据抓取/样例文件，当前未接入网页运行时渲染。

`fieldsight.html` 的天气数据运行时能力已较完整；后续重点应放在气候指数、新闻、事件与策略事实的动态化和可审核化。

---

## 已具备动态能力

### 1. 产区实时天气、预报与风险计算

页面会按照产区代表站的经纬度请求 Open-Meteo，在浏览器端动态计算并展示：

- 近 37 天平均气温与累计降水；
- 最高/最低气温；
- 未来 7 天降水和最高温预报；
- 干旱连续天数；
- 积温与有效积温；
- 高温、强降水、干旱、霜冻等风险标签；
- 请求超时、重试、并发控制，以及 Archive 历史库降级。

相关实现：

- `fieldsight.html`：气象窗口、请求、指标计算、风险判定、结果渲染。
- `index.html`：保留了相近的运行时天气与风险计算逻辑。

### 2. 季节性降水与气温图表

打开产区季节性分析弹窗时，页面按需请求 2016 年以来的历史日值，并在浏览器端计算：

- 当年累计降水；
- 历史同期最高—最低区间；
- 多年平均；
- 近 5 年平均；
- 当年温度 7 天滑动平均。

该功能已动态化，但缓存仅存在当前浏览器内存中；刷新后需要重新请求。当前按需加载的方式适合单站点分析，不适合页面初始化时批量预加载。

### 3. 中国、美国天气图与台风图层

以下内容主要通过官方图源或嵌入页面动态更新：

- NOAA / WPC / SPC / CPC / NWS / USDM 天气图；
- 中国气象台降水、土壤墒情、温度、雷达、卫星与强对流产品；
- World Ag Weather / GEFS 图集；
- 台风实时地图与官方路径页。

目前前端通过固定 URL、时间戳和图片可用性探测来展示最新内容。

> 注意：此类动态更新依赖第三方 URL 规则、跨域限制与站点策略。NMC、WAW 等图源若调整文件路径、时间编号或嵌入策略，页面可能出现降级或空白。

---

## 仍为静态或半静态的内容

### P0：策略建议中的市场事实与研判文字

策略区会读取实时天气指标，但其中不少事实性描述仍是前端硬编码内容，例如：

- 作物优良率；
- 出口、库存等基本面数据；
- 期货价格、涨跌和原油/生柴关联市场信息；
- ENSO 定性结论；
- 品种偏多、偏空等判断背景。

因此当前策略属于“**动态天气输入 + 静态市场叙述**”的混合形式。

#### 建议方案

将策略输入拆分为四类：

1. 动态气象指标；
2. 动态基本面指标；
3. 有来源和版本记录的气候指数；
4. 可审计、可配置的策略规则。

推荐由服务端生成 `strategy-inputs.json`，并为每项输入保留：

```json
{
  "value": 63,
  "unit": "%",
  "observed_at": "2026-07-29",
  "source_url": "https://example.com/source",
  "fetched_at": "2026-07-29T08:00:00Z",
  "status": "reviewed"
}
```

前端仅负责确定性展示和规则评分。关键数据过期或缺失时，应显示“数据过期”或“不生成策略建议”，而不应继续使用旧事实输出强结论。

> 该项价值最高、难度也最高，涉及授权、数据口径、更新时间、事实核验与研究表述边界。

### P1：ENSO、PDO、NAO、IOD 与气候概览

当前海洋指数卡、ENSO 概览、气候预警和农业影响解释主要来自 `fieldsight-data.js` 中人工维护的静态数据。

#### 建议方案

建立定时采集与审核链路：

```text
NOAA CPC / BOM / JMA 等官方原始数据
→ 定时抓取、清洗、统一口径
→ 版本化 JSON
→ 人工审核解释性结论
→ 页面展示数值、来源和更新时间
```

建议每个指数至少包含：

- `value`：指数值；
- `unit`：单位；
- `observed_at`：观测期；
- `source_url`：官方来源；
- `fetched_at`：抓取时间；
- `reviewed_at`：审核时间；
- `status`：审核/待审核/失效状态。

指数数值可自动更新；“拉尼娜是否确立”“对农业的具体影响”等解释性结论建议保留人工审核。

### P1：农业新闻

当前新闻由 `fieldsight-data.js` 静态数组提供；页面仅按照本机时间过滤过期新闻，并不会主动拉取最新资讯。

#### 建议方案

```text
白名单 RSS / 官方公告 / 可授权新闻 API
→ 服务端定时抓取
→ 时区统一、去重、区域与品种标签化
→ 短时缓存 JSON
→ 前端展示
```

建议优先接入 USDA、FAO、NOAA、NMC 等官方与稳定来源；媒体新闻需处理版权、反爬、转载重复、链接失效与自动摘要失真的问题。

### P1：特殊天气事件

台风、高温、强降水、霜冻等事件卡片目前由静态数据维护，事件标题、状态、影响和研判均不会自动更新。

#### 建议方案：两层事件机制

1. **自动候选层**
   - 官方预警；
   - 气温、降水、风速等阈值；
   - 热带气旋路径；
   - 干旱或降水异常。

2. **人工发布层**
   - 确认是否需要进入研究面板；
   - 编写作物、产区与供应链影响说明；
   - 标注影响等级、时效和数据来源。

这样可以降低人工筛查成本，同时避免单站点或单阈值触发误报。

### P1：作物物候、生育期与代表站配置

以下内容目前为静态配置：

- 产区代表站经纬度；
- 作物区域映射；
- 积温起算日期 `gddStart`；
- 生育期、敏感期和风险说明；
- ENSO 对作物的影响描述。

当前实时更新的是代表站天气，并不意味着作物实际物候也会同步更新。

#### 演进路线

| 阶段 | 做法 | 难度 |
|---|---|---|
| 短期 | 从 JS 常量迁移到独立 JSON / 数据库，支持季度或季节批量维护 | 低 |
| 中期 | 使用播期、积温和区域作物历计算物候阶段 | 中 |
| 长期 | 接入遥感 NDVI/LAI、作物面积与官方生育进度数据 | 高 |

> 页面应明确定位为“代表站气象监测”，不能直接等同为整个产区的作物长势或产量判断。

### P2：NMC 与 WAW 图集探测

当前图像内容会更新，但浏览器端依赖 URL 模板、时间戳和图片探测来寻找可用资源。

长期建议改为：

```text
浏览器逐项探测图片
→ 服务端定时探测与缓存最新可用 URL
→ 前端读取稳定的图源清单
```

可集中处理图源路径变更、可用性监控、时间错位、跨域失败和降级链接，减少每个用户打开页面时的探测请求。

---

## CSV 与 Python 脚本状态

根目录 CSV 当前是离线样例，并未被网页读取：

- `菏泽_近37天天气数据.csv`；
- `Iowa_2022年全年降水.csv`；
- `Iowa_2022年降水_NOAA_CPC.csv`。

`fetch_data.py` 可手动请求 Open-Meteo 并覆盖生成 CSV，但目前没有：

- 定时任务；
- 服务端接口；
- 前端消费链路；
- 自动数据新鲜度校验。

后续应明确选择其中一种路线：

1. **离线样例模式**：文档明确说明页面不读取该数据，也不保证实时性；
2. **正式数据链路模式**：定时运行脚本，输出 JSON/API，前端读取接口；CSV 仅作为导出格式。

不建议维持当前模糊状态，避免用户误以为 CSV 就是页面实时数据来源。

---

## `index.html` 与 `fieldsight.html` 的版本治理

两个入口都具备实时天气能力，但静态研究内容存在版本漂移：

- `fieldsight.html` 使用较新的 `fieldsight-data.js`；
- `index.html` 使用较早的 `data.js`；
- 两页的 ENSO、新闻和策略文字可能不一致。

建议：

1. 明确 `fieldsight.html` 为当前主入口；
2. 将 `index.html` 标记为旧版或备用看板；或
3. 将两页共享内容抽成统一 JSON/API，避免双维护。

---

## 推荐实施优先级

### 第一阶段：低风险、高价值

1. 海洋指数数值改为定时生成的 JSON；
2. 新闻改为服务端 RSS/API 聚合；
3. 将 JS 内静态研究内容拆出为版本化数据文件；
4. 为动态内容补充来源、观测时间、抓取时间和降级状态。

### 第二阶段：研究内容可维护化

5. 特殊天气事件改为“自动候选 + 人工审核”；
6. 物候、生育期和代表站配置改为可维护的数据层；
7. 策略区拆分为“动态事实输入 + 确定性规则输出”。

### 第三阶段：稳定性与性能

8. 为 Open-Meteo、NMC、WAW 图源增加服务端缓存；
9. 建立统一 API，解决两个页面之间的数据版本漂移；
10. 将 CSV 升级为可导出数据产物，或明确其离线归档用途。

---

## 本次前端视觉更新记录

- `fieldsight.html` 已由深色终端风改为白色机构研究简报风；
- 统一使用雾白页面、白色内容面、浅灰绿辅助面和深墨绿正文；
- 建发农产品 Logo 已加入报头与页脚；
- 章节、指标、图集、表格、新闻、事件、策略、台风和弹窗均已适配白底视觉体系；
- 季节性图表使用白底可读主题：历史区间为淡蓝阴影、多年均值为橙色虚线、近 5 年均值为蓝线、当年为绿线；
- 静态检查、数据脚本语法检查、关键 DOM 检查和本地服务资源检查均已通过。

## 后续待处理的小项

季节性图表弹窗中的部分说明文字仍沿用旧版颜色描述，应改为按系列语义说明，避免写死颜色。例如：

```text
阴影=历史同期区间 · 虚线=多年均值 · 蓝线=近5年均值 · 绿线=今年
```

或使用不依赖颜色的描述：

```text
当前年度曲线高于/低于历史区间，表示同期偏湿/偏旱。
```
