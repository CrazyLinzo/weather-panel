# memo · 降水精度调研 + 多点聚合方案 + 配额评估

> 8.10 记录「Open-Meteo 降水准确性」「多点聚合可行性」「静态锁定 + 每日增量架构」「API 配额」的讨论与结论；8.11 已将「静态锁定 + 每日增量」在官方数据层落地验证（见第四节 ✅ 段）。本文作为**未来优化路线图**持续更新。

**更新记录**：2026-08-10 成文 · 2026-08-11 追加官方数据层自动化落地衔接、清理 data.js 旧版引用

---

## 一、讨论背景

用户感觉 Open-Meteo 的降水数据不准确，对照了两组数据：

- **NOAA CPC Unified Precipitation**（`https://ftp.cpc.ncep.noaa.gov/precip/CPC_UNI_PRCP/`）——雨量计网格化分析，业内当作"观测真值"；
- **路透 Reuters API**——依赖其监测站网络。

由此追问：Open-Meteo 降水怎么算的？ERA5 是什么、覆盖面多广？Iowa/黑龙江这种大面积数据是区域拟合还是单点？做多点聚合工程量多大？Open-Meteo 支持不支持？

---

## 二、调研结论（已核实）

### 1. Open-Meteo 降水的计算逻辑

| 接口 | 数据性质 |
|---|---|
| `api.open-meteo.com/v1/forecast`（`past_days`） | **NWP 数值模式输出**（GFS/ICON 等），`past_days` 是模式回溯，**不是观测** |
| `archive-api.open-meteo.com/v1/archive` | **ERA5 再分析**，模式 + 多源观测同化 |

> ERA5-Land（0.1°）经 Open-Meteo **不提供降水**，会自动回退到 ERA5。

### 2. ERA5 不是卫星数据 ⚠️（纠偏）

ERA5 是**再分析**：数值模式 + 多源观测同化（地面站、探空、飞机、浮标、雷达、卫星）。降水部分是**模式生成**（对流参数化），不是纯卫星反演。

- 分辨率 **0.25° ≈ 20–28 km × 28 km ≈ 540–600 km²**（中纬度）
- 覆盖面广、能补站点稀疏地区、可提供趋势性数据——**这个理解正确**
- 但不能当观测真值用，站点密度高的地区应以 CPC 等雨量计网格为准

### 3. 大面积数据是单点还是区域拟合？——单点

看板用**每个产区一个代表点**请求：

- `fieldsight-data.js`：**172 点**（旧版 `data.js` 107 点已于 8/11 清理删除）

例如 Iowa 用得梅因（41.59, -93.62），黑龙江用哈尔滨（45.75, 126.63）。**不是区域拟合**，是一个站点的值代表整个产区。

### 4. 网格面积换算

- 1° 纬度 ≈ 111 km
- 1° 经度 ≈ 111 × cos(纬度) km
- 0.25° 网格单元 ≈ 540–600 km²；CPC 0.5° 单元 ≈ 2,300 km²

### 5. Open-Meteo 能力边界（官方文档核实）

| 能力 | 是否支持 |
|---|---|
| 一次请求多点（逗号分隔经纬度，返回数组） | ✅ 原生支持 |
| 批量网格坐标生成（bbox 输入辅助） | ✅（仅生成坐标列表，非面平均） |
| **服务端面积平均 / 多边形聚合** | ❌ **不支持，必须自己算** |

> 官方文档搜不到 "area-average" / "polygon"；"Bounding box" 只是把框展开成坐标列表的输入方式，返回仍是逐点值。

---

## 三、多点聚合方案（工程评估）

对全部产区多点铺格、取平均值：

| 方案 | 做法 | 工程量 |
|---|---|---|
| A | bbox 自动铺格 + 客户端聚合 | ~2.5–4 人天 |
| B | 真实边界 + 服务端抓取聚合 | ~4–6 人天 |

- 网格推荐 **0.25° 间距**（匹配 ERA5 分辨率，兼顾数值质量与点数）
- 主风险：**免费层配额**（见第六节）

---

## 四、静态锁定 + 每日增量架构（用户提出，结论：可行）

用户思路：**历史数据一旦过今天就不可变，一次性下载锁定进仓库当静态；以后只更新每日增量，数据量会小很多。** —— 正确，正是 GitHub Pages + Actions 的标准做法。

```
┌─ 静态历史层（一次性，锁定，永不重拉）───────┐
│ 今天之前的数据：下载一次 → 提交进仓库 → JSON  │
│ 由 GitHub Pages 托管，浏览器读本地，0 次 API   │
└──────────────────────────────────────────┘
┌─ 动态增量层（每天一次，量极小）─────────────┐
│ GitHub Actions 定时任务（每日 00:30 左右）    │
│ 只拉"今天新增的 1 天 + 刷新未来 7 天预报"     │
│ 合并进当前 JSON → 提交 → Pages 自动重建      │
└──────────────────────────────────────────┘
```

### 三个真实坑

1. **预报部分锁不了**：未来 7 天预报每天被新模型运行覆盖，必须每天重拉（量小，无所谓）。
2. **最近 ~5 天历史会被修订**：ERA5 最近 5 天是初值（ERA5T），之后被正式版替换。对策：锁定到"今天−5 天"，最近 5 天用 forecast 的 `past_days` 补；或接受边界小幅修订（对 37 天指标影响可忽略）。
3. **回填别贪十年**：十年季节性历史**维持单点**（现状即可）；网格回填只做**近 44 天窗口**（37 天指标 + 冗余），避免仓库暴涨。

### ✅ 已落地验证（2026-08-11）——官方数据层自动化

本节的「静态锁定 + 每日增量」已在天气看板**官方数据层**完整落地（对象是官方文本/指数数据，非降水，但同一架构），可直接复用为降水聚合模板：

- GitHub Actions `daily-fetch.yml` 每日 00:17 自动运行 `fetch_official.py → parse_midrange.py → fetch_usda.py → probe_events.py → probe_sources.py`；
- 输出 `data/*.js` 静态 JSON（auto-fetch / crop-progress / cn-outlook / special-events / img-sources / event-candidates），自动 commit，Pages 自重建，页面加载 0 次外部 API；
- 前端 `typeof` 守卫：自动文件缺失时回退人工层 `fieldsight-data.js`，页面不破。

**降水复用模板**：按同一模式写 `probe_region_precip.py` → 生成 `data/regions-precip.js` → 前端守卫回退当前单点值。唯一差异：降水要过 API 配额关（见第五、六节），`X-Pos-Count` 实测决定网格密度。

---

## 五、配额实测（官方定价页 + GitHub issue 核实）

### 免费层限额

| 维度 | Free | API Standard | API Professional | API Enterprise |
|---|---|---|---|---|
| 每分钟 | 600 | 不限 | 不限 | 不限 |
| 每小时 | 5,000 | 不限 | 不限 | 不限 |
| 每天 | **10,000** | 不限 | 不限 | 不限 |
| 每月 | **300,000** | 100 万 | 500 万 | >5000 万 |
| 商用授权 | ❌ | ✅ | ✅ | ✅ |

### 计数方式：1 次请求 ≠ 1 次 call ⚠️

按**数据量加权**计数：位置数 × 天数 × 变量数（× 模型数）。官方 [GitHub issue #485](https://github.com/open-meteo/open-meteo/issues/485) 实锤：**一次 400 点 × 日值+小时 × 全模型 的请求，直接打爆 10,000 日限额**。

> 精确权重系数官方未公开（服务端闭源），但响应头带真实计数：`X-Pos-Count` / `X-Max-Pos-Count` / `X-Pos-Count-24h`，**跑一次就能读出真实消耗**。

### 各场景账（假设权重 ≈ 位置 × 天数 × 变量）

| 场景 | 权重估算 | 10k/天 | 300k/月 |
|---|---|---|---|
| 现状每次开页面：172×99 天×3 变量 | ~51k | ❌ 5 倍爆 | ❌ |
| 每日增量·单点：172×8×3 | ~4.1k | ✅ | ✅（123k/月） |
| 每日增量·9 点网格：1548×8×3 | ~37k | ❌ 3.7 倍 | ❌（1.1M/月） |
| 一次性回填 1548×44×3 | ~204k | ❌ 拆 ~21 天 | ⚠️ 占 2/3 |

### 出路

- 方案一：**降网格密度**（每产区 3~4 点 → 仍超，需 ~2 点/区）；
- 方案二：**升级 API Standard（100 万次/月）**，密网格 1.1M/月刚好兜住；
- 关键前提：若真实权重系数 < 1（历史上数次下调过），密网格每日增量可能回到免费层可承受区间——**需实测后定**。

---

## 六、结论

1. 降水不准的根因：**模式/再分析（Open-Meteo）vs 雨量计网格（CPC）** + **单点 vs 0.5° 网格** 的口径差异，属正常系统性偏差，不是 bug。
2. **多点聚合在 Open-Meteo 技术上可行**（一次请求多点原生支持），但面积平均必须自己算。
3. **用户提出的"静态锁定 + 每日增量"方案成立**，能把现状每次开页面 ~51k 的消耗归零，是整套方案可行性的关键。
4. 唯一硬约束是**免费层配额**（10k/天 / 300k/月）对密网格的承载力，需要用真实权重系数校验。

---

## 七、未来路线图（截至 2026-08-11）

**✅ 已完成**
- 官方数据层自动化落地：第四节架构已验证（见上 ✅ 段），Actions 每日构建 + 静态 JSON + typeof 回退，模板可复用。

**🕒 待做 —— 降水多点聚合（本 memo 主课题）**
1. **原型验证（先做）**：Iowa + 黑龙江各铺 9 点，跑通全链路：
   - 铺点脚本 → 一次性回填 → 静态 JSON → 页面读静态 → 每日增量脚本；
   - **第一件事从响应头读真实 call 消耗**（`X-Pos-Count` / `X-Pos-Count-24h`），用实测系数决定网格密度与是否升级；
   - 拿实测值与单点、以及 NOAA CPC 下载数据对比，验证改进幅度。
2. **全量推广**：原型 OK 后铺到全部 172 产区（网格 + 聚合指标）。
3. **工程化**：直接复用官方数据层模板（`probe_region_precip.py` → `data/regions-precip.js` → typeof 守卫），页面加载 0 API 调用。

**📌 顺带（相邻项目，勿混）**：天气日报脚本版本问题（v4.3 vs 计划任务指向的 demo4.0）待修——属谷物天气日报项目，与天气看板无关。
